import { EffectComponent } from '../../lib/effect_component.ts'

export { EffectComponent }

/**
 * Base class for property editor sub-components.
 * Extends EffectComponent — render(), get(), getTemplate(), addEffect() all inherited.
 */
export class BasePropertyElement extends EffectComponent {}
